
import 'package:flutter/material.dart';

class favs extends StatelessWidget {
  const favs({super.key});
  
  @override
  Widget build(BuildContext context) {
    
    List<String> favs_list;
    return Container(



      
    );
  }

  fetchfavs(){
    // Todo: Implemment the fetch of id's
  }
}